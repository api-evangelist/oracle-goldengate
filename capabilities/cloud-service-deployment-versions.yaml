naftiko: 1.0.0-alpha2
info:
  label: Oracle GoldenGate Cloud Service API — Deployment Versions
  description: 'Oracle GoldenGate Cloud Service API — Deployment Versions. 1 operations. Lead operation: Oracle Goldengate
    List Deployment Versions. Self-contained Naftiko capability covering one Oracle Goldengate business surface.'
  tags:
  - Oracle Goldengate
  - Deployment Versions
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ORACLE_GOLDENGATE_API_KEY: ORACLE_GOLDENGATE_API_KEY
capability:
  consumes:
  - type: http
    namespace: cloud-service-deployment-versions
    baseUri: https://goldengate.{region}.oci.oraclecloud.com
    description: Oracle GoldenGate Cloud Service API — Deployment Versions business capability. Self-contained, no shared
      references.
    resources:
    - name: 20200407-deploymentVersions
      path: /20200407/deploymentVersions
      operations:
      - name: listdeploymentversions
        method: GET
        description: Oracle Goldengate List Deployment Versions
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.ORACLE_GOLDENGATE_API_KEY}}'
  exposes:
  - type: rest
    namespace: cloud-service-deployment-versions-rest
    port: 8080
    description: REST adapter for Oracle GoldenGate Cloud Service API — Deployment Versions. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/20200407/deploymentversions
      name: 20200407-deploymentversions
      description: REST surface for 20200407-deploymentVersions.
      operations:
      - method: GET
        name: listdeploymentversions
        description: Oracle Goldengate List Deployment Versions
        call: cloud-service-deployment-versions.listdeploymentversions
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: cloud-service-deployment-versions-mcp
    port: 9090
    transport: http
    description: MCP adapter for Oracle GoldenGate Cloud Service API — Deployment Versions. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: oracle-goldengate-list-deployment-versions
      description: Oracle Goldengate List Deployment Versions
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: cloud-service-deployment-versions.listdeploymentversions
      outputParameters:
      - type: object
        mapping: $.
