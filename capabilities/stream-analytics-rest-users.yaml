naftiko: 1.0.0-alpha2
info:
  label: Oracle GoldenGate Stream Analytics REST API — Users
  description: 'Oracle GoldenGate Stream Analytics REST API — Users. 4 operations. Lead operation: Oracle Goldengate List
    Users for a Role. Self-contained Naftiko capability covering one Oracle Goldengate business surface.'
  tags:
  - Oracle Goldengate
  - Users
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ORACLE_GOLDENGATE_API_KEY: ORACLE_GOLDENGATE_API_KEY
capability:
  consumes:
  - type: http
    namespace: stream-analytics-rest-users
    baseUri: https://{ggsa-host}:{port}/osa
    description: Oracle GoldenGate Stream Analytics REST API — Users business capability. Self-contained, no shared references.
    resources:
    - name: services-v1-authorizations-role
      path: /services/v1/authorizations/{role}
      operations:
      - name: listusers
        method: GET
        description: Oracle Goldengate List Users for a Role
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: services-v1-authorizations-role-user
      path: /services/v1/authorizations/{role}/{user}
      operations:
      - name: getuser
        method: GET
        description: Oracle Goldengate Retrieve a User
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: createuser
        method: POST
        description: Oracle Goldengate Create a User
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: updateuser
        method: PATCH
        description: Oracle Goldengate Update a User
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: basic
      username: '{{env.ORACLE_GOLDENGATE_USER}}'
      password: '{{env.ORACLE_GOLDENGATE_PASS}}'
  exposes:
  - type: rest
    namespace: stream-analytics-rest-users-rest
    port: 8080
    description: REST adapter for Oracle GoldenGate Stream Analytics REST API — Users. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/services/v1/authorizations/{role}
      name: services-v1-authorizations-role
      description: REST surface for services-v1-authorizations-role.
      operations:
      - method: GET
        name: listusers
        description: Oracle Goldengate List Users for a Role
        call: stream-analytics-rest-users.listusers
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/services/v1/authorizations/{role}/{user}
      name: services-v1-authorizations-role-user
      description: REST surface for services-v1-authorizations-role-user.
      operations:
      - method: GET
        name: getuser
        description: Oracle Goldengate Retrieve a User
        call: stream-analytics-rest-users.getuser
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createuser
        description: Oracle Goldengate Create a User
        call: stream-analytics-rest-users.createuser
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: updateuser
        description: Oracle Goldengate Update a User
        call: stream-analytics-rest-users.updateuser
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: stream-analytics-rest-users-mcp
    port: 9090
    transport: http
    description: MCP adapter for Oracle GoldenGate Stream Analytics REST API — Users. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: oracle-goldengate-list-users-role
      description: Oracle Goldengate List Users for a Role
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: stream-analytics-rest-users.listusers
      outputParameters:
      - type: object
        mapping: $.
    - name: oracle-goldengate-retrieve-user
      description: Oracle Goldengate Retrieve a User
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: stream-analytics-rest-users.getuser
      outputParameters:
      - type: object
        mapping: $.
    - name: oracle-goldengate-create-user
      description: Oracle Goldengate Create a User
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: stream-analytics-rest-users.createuser
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: oracle-goldengate-update-user
      description: Oracle Goldengate Update a User
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: stream-analytics-rest-users.updateuser
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
